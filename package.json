{
  "name": "media-archiver-userscript",
  "version": "6.0.0",
  "private": true,
  "description": "Build and validation tooling for the modular Media Archiver product.",
  "scripts": {
    "build": "node scripts/assemble-userscript.mjs",
    "build:userscript": "node scripts/assemble-userscript.mjs",
    "check:syntax": "node --check media-archiver.user.js",
    "check:invariants": "node scripts/check-userscript.mjs",
    "check:unsupported": "node scripts/smoke-unsupported-page.mjs",
    "test:baseline": "node --test tests/baseline.test.mjs",
    "test:unit": "node --test tests/contracts.test.mjs tests/shared-services.test.mjs",
    "test": "npm run build:userscript && npm run check:syntax && npm run check:invariants && npm run check:unsupported && npm run test:baseline && npm run test:unit"
  },
  "engines": {
    "node": ">=20"
  }
}
