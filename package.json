{
  "name": "media-archiver-userscript",
  "version": "6.0.0",
  "private": true,
  "description": "Build and validation tooling for the modular Media Archiver userscript.",
  "scripts": {
    "build": "node scripts/assemble-userscript.mjs",
    "check:syntax": "node --check media-archiver.user.js",
    "check:invariants": "node scripts/check-userscript.mjs",
    "test": "npm run build && npm run check:syntax && npm run check:invariants && npm run check:unsupported",
    "check:unsupported": "node scripts/smoke-unsupported-page.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
